
server.port=8084
spring.application.name=Candidate-Service

#Confiq
spring.config.import=configserver:http://localhost:8080/

#MySQL
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:candidate}
